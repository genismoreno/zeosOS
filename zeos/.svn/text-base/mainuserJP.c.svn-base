//CHECKPOINT 1

//make_writes();
	//print_time(15);
	//runjp_rank(1, 9);
	/*char *buffer = "It is a long established fact that a reader will be distracted"
	"by the readable content of a page when looking at its layout. The point of using"
	"Lorem Ipsum is that it has a more-or-less normal distribution of letters, as" 
	"pposed to using Content here, content here, making it look like readable"
	"English. Many desktop publishing packages and web page editors now use Lorem"
	"Ipsum as their default model text, and a search for lorem ipsum will" 
	"uncover many web sites still in their infancy. Various versions have evolved"
	"over the years, sometimes by accident, sometimes on purpose (injected humour"
	"and the like)";
	int k = strlen(buffer);
	int m = write(1, buffer, strlen(buffer));
	write(1, "\nVAL:", 5);
	char buffd[20];
	itoa(k, buffd);
	write(1, buffd, 3);
	itoa(m, buffd);
	write(1, ", ", 2); 
	write(1, buffd, strlen(buffd));
	*/

//ENTREGA 1
runjp();
	/*int i = fork();
	if (i == 0) {
		write(1, "soc el fill\n", 12);
		i = getpid();
		char buffa[20];
		itoa(i, buffa);
		write(1, buffa, strlen(buffa));
		exit();
	}
	else {
		write(1, "soc el pare\n", 12);
		i =getpid();
		char buffd[20];
		itoa(i, buffd);
		write(1, buffd, strlen(buffd));
		write(1,"\n",1);
		exit();
	}
	int pid = getpid();
	//struct stats buffstats;
	//get_stats(pid, buffstats);
	//write(1, buffstats, strlen(buffstats));
	//write(1,"\n",1);*/
